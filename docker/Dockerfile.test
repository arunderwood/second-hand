# Dockerfile for integration testing with chronyd
FROM fedora:41

# Install chrony and Python
RUN dnf install -y \
    chrony \
    python3.14 \
    python3-pip \
    && dnf clean all

# Create chrony directories
RUN mkdir -p /run/chrony /var/lib/chrony /var/log/chrony \
    && chown chrony:chrony /run/chrony /var/lib/chrony /var/log/chrony

# Copy chrony configuration
COPY docker/chrony.conf /etc/chrony.conf

# Install uv for dependency management
RUN pip install uv

# Set up working directory
WORKDIR /app

# Copy project files
COPY pyproject.toml uv.lock* ./
COPY src/ ./src/
COPY tests/ ./tests/

# Install dependencies
RUN uv sync --all-extras --dev

# Set PYTHONPATH
ENV PYTHONPATH=/app/src

# Default command runs integration tests
CMD ["sh", "-c", "chronyd && sleep 2 && uv run pytest tests/integration -v"]
